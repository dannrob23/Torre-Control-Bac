# ---------------------------------------------------------------------------
# Torre de Control SLA - dependencias para Streamlit Cloud
#
# Se instalan automaticamente al desplegar. Solo lo que el DASHBOARD necesita:
# nada de tkinter, plyer ni PowerShell (eso es de escritorio Windows).
# ---------------------------------------------------------------------------

# Nucleo: lectura del Excel y calculo de SLA
pandas>=2.0,<3.0
openpyxl>=3.1,<4.0

# Dashboard web
streamlit>=1.30

# Envio a Telegram (opcional: si no se configura, el boton no aparece)
requests>=2.31

# Control de acceso: login con usuario y contrasena (auth.py).
# Arrastra bcrypt, pyjwt, cryptography, pyyaml y extra-streamlit-components.
streamlit-authenticator>=0.4.2

# Lectura/escritura del archivo de secretos (configurar_acceso.py)
toml>=0.10
